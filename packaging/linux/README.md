# Linux Packaging

This directory is reserved for future Linux distribution work.

Suggested subdirectories:

- `deb/`
- `rpm/`
- `arch/`

Keep Linux package metadata here once the repo is ready to define Linux packaging and runtime contracts.
