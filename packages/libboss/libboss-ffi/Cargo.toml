[package]
name = "libboss-ffi"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "libboss_ffi"
path = "src/lib.rs"
crate-type = ["staticlib", "cdylib"]

[dependencies]
async-trait = "0.1"
futures = "0.3"
libboss-core = { path = "../libboss-core" }
libboss-session = { path = "../libboss-session" }
