[package]
name = "libboss-session"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "libboss_session"
path = "src/lib.rs"

[dependencies]
async-trait = "0.1"
libboss-core = { path = "../libboss-core" }

[dev-dependencies]
futures = "0.3"
